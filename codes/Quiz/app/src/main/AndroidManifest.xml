<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="su.quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity android:name=".QMActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".QTActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".AddQuestionsActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".QuestionsManagementActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".CheckQuestionActivity"
            android:label="@string/app_name">

        </activity>


        <activity android:name=".TakerManagementActivity"
        android:label="@string/app_name">

        </activity>

        <activity android:name=".AddTakersActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".CheckTakerActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".StatisticManagementActivity"
            android:label="@string/app_name">

        </activity>

    </application>

     <uses-permission android:name="android.permission.VIBRATE" />

</manifest>